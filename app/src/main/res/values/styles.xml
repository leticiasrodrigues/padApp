<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="cursiveText">
        <item name="android:fontFamily">cursive</item>
    </style>

    <style name="mainTitle" parent="cursiveText">
        <item name="android:textColor">@color/blueDark</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/main_title</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@color/blue</item>
        <item name="android:paddingBottom">@dimen/main_title_horizontal_padding</item>
        <item name="android:paddingTop">@dimen/main_title_horizontal_padding</item>
        <item name="android:paddingLeft">@dimen/activity_vertical_margin</item>
        <item name="android:paddingRight">@dimen/activity_vertical_margin</item>
    </style>

    <style name="padName" parent="cursiveText">
        <item name="android:textColor">@color/blueLight</item>
        <item name="android:textSize">@dimen/pad_name</item>
        <item name="android:background">@color/blueDark</item>
        <item name="android:paddingLeft">@dimen/pad_name_vertical_padding</item>
        <item name="android:paddingRight">@dimen/pad_name_vertical_padding</item>
        <item name="android:paddingTop">@dimen/pad_name_horizontal_padding</item>
        <item name="android:paddingBottom">@dimen/pad_name_horizontal_padding</item>
    </style>

</resources>
